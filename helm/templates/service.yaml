apiVersion: v1
kind: Service
metadata:
  name: spring-svc
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: spring-app
  ports:
    - port: 80         # ALB/Ingress가 호출할 포트
      targetPort: 8080 # Spring 컨테이너 포트
